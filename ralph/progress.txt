# Ralph Progress Log
Started: 2026-03-04
---

## Codebase Patterns
- Project root is `agent-observatory/` (not `ralph/`); all code/docs changes go there
- Branch created from `main` in the `agentew_valley` monorepo root
- Typecheck: `pnpm --filter @agent-observatory/server build` (tsc); web package has a pre-existing vite config TS error unrelated to migration work
- docs/migration/ is created fresh; no prior content exists
- Migration governance docs should prefer measurable acceptance tables (`Target`, `Measurement Source`, `Alert Threshold`) to keep criteria verifiable
- Ops-domain schema guidance should enforce shared audit/tenant columns (`workspace_id`, `created_at`, `updated_at`, `actor`) and keep UAEP telemetry storage boundary separate from operational source-of-truth tables
- Backfill planning docs should define per-entity source->target mappings together with field-level transform/null rules and explicit skip-code taxonomy for unsupported records
---

## 2026-03-04 - US-001
- Created `agent-observatory/docs/migration/scope-cutline.md`
- Sections: Parity, Equivalent, New; Domain classification table (12 rows covering all 7 required domains plus additional ones); explicit In-Scope and Out-of-Scope lists
- Files changed: `docs/migration/scope-cutline.md` (new)
- **Learnings for future iterations:**
  - The `docs/migration/` directory must be created manually (mkdir -p)
  - The web package's vite.config.ts has a pre-existing TS error (`test` property unknown); only server/shared/collectors typechecks are meaningful for migration work
  - US-002 through US-005 also create files in `docs/migration/`; US-006 creates `scripts/migration/`; US-007 and later touch `packages/server/src/`
---

## 2026-03-04 16:21:33 KST - US-002
- Created `agent-observatory/docs/migration/slo-targets.md` with explicit migration SLO targets for API p95 latency, WebSocket reconnect time, event loss rate, RPO, RTO, and retention.
- Added per-metric measurement source and alert threshold definitions, plus monthly error budgets and a breach response policy with escalation and rollback safeguards.
- Files changed: `agent-observatory/docs/migration/slo-targets.md`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: Migration governance docs in `docs/migration/` should use measurable tables (target, measurement source, alert threshold) so acceptance can be objectively verified.
  - Gotchas encountered: `prd.json` and `progress.txt` live under `ralph/`, but implementation files for this project live under `agent-observatory/`.
  - Useful context: This story is docs-only; browser verification is not applicable unless a later story introduces UI changes.
---

## 2026-03-04 16:26:29 KST - US-003
- Created `agent-observatory/docs/migration/data-model-principles.md` defining the UAEP event store vs ops domain store boundary and explicit placement rules for operational state.
- Added required shared ops-table columns (`workspace_id`, `created_at`, `updated_at`, `actor`) with behavior requirements, plus naming/indexing/foreign-key conventions and a merge checklist.
- Files changed: `agent-observatory/docs/migration/data-model-principles.md`, `agent-observatory/CLAUDE.md`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: For migration-era relational schemas, treat UAEP as append-only telemetry and keep business state authoritative in ops-domain tables.
  - Gotchas encountered: Shared ops columns should define write semantics (`created_at` immutable, `updated_at` updated on mutation, `actor` explicit on write) to avoid inconsistent audit behavior.
  - Useful context: Existing server SQL style uses `idx_<table>_<columns>` names and `IF NOT EXISTS`; documenting the same convention keeps future migrations consistent.
---

## 2026-03-04 16:29:59 KST - US-004
- Created `agent-observatory/docs/migration/backfill-plan.md` with source-to-target mapping coverage for Tasks, Reviews, Notifications, Activities, and Webhooks.
- Added field-level transformation and null-handling policies for each entity, plus unsupported-record handling rules and a skip-code taxonomy with exception reporting requirements.
- Files changed: `agent-observatory/docs/migration/backfill-plan.md`, `agent-observatory/CLAUDE.md`, `ralph/prd.json`, `ralph/progress.txt`
- **Learnings for future iterations:**
  - Patterns discovered: Backfill docs are most reviewable when each entity has a dedicated mapping table and explicit null/default behavior per field.
  - Gotchas encountered: Skip policy must distinguish hard integrity blockers (skip) from nullable business states (allow null/default) to avoid silently dropping valid records.
  - Useful context: Idempotency key and upsert conflict semantics were intentionally deferred to US-005; this file currently establishes only mapping/transform/null/skip baseline.
---
